#include<bits/stdc++.h>
using namespace std;
int main()
{
  int n;
  string temp;
  cin>>n;
  vector<string> grid(n);
  for(int i=0;i<n;i++) cin>>grid[i];
  for(int i=0;i<n;i++)
  {
    for(int j=0;j<n;j++)
    {
      temp=grid[i];
      if(temp[j])
    }
  }

  return 0;

}
